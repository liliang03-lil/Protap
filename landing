<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ProTap — La carte NFC qui transforme vos contacts en clients</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --gold: #C9A84C;
      --gold-light: #E8C97A;
      --dark: #0d1621;
      --dark2: #15202e;
      --dark3: #1c2b3a;
      --text: #F0EDE6;
      --text-muted: #8A8478;
      --border: rgba(201,168,76,0.18);
      --green: #4CAF7D;
    }
    html { scroll-behavior: smooth; }
    body { background: var(--dark); color: var(--text); font-family: 'DM Sans', sans-serif; font-weight: 300; -webkit-font-smoothing: antialiased; overflow-x: hidden; }

    /* NAV */
    nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100; padding: 20px 40px; display: flex; align-items: center; justify-content: space-between; background: rgba(13,22,33,0.85); backdrop-filter: blur(12px); border-bottom: 1px solid var(--border); }
    .nav-logo { font-family: 'Cormorant Garamond', serif; font-size: 22px; font-weight: 400; color: var(--text); text-decoration: none; }
    .nav-logo span { color: var(--gold); }
    .nav-links { display: flex; gap: 32px; align-items: center; }
    .nav-links a { font-size: 13px; color: var(--text-muted); text-decoration: none; letter-spacing: 0.05em; transition: color 0.2s; }
    .nav-links a:hover { color: var(--gold); }
    .nav-cta { padding: 10px 22px; background: linear-gradient(135deg, var(--gold), var(--gold-light)); border-radius: 8px; color: var(--dark); font-size: 12px; font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; transition: opacity 0.2s; }
    .nav-cta:hover { opacity: 0.85; color: var(--dark); }

    /* HERO */
    .hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 120px 40px 80px; text-align: center; position: relative; overflow: hidden; }
    .hero::before { content: ''; position: absolute; inset: 0; background: radial-gradient(ellipse 80% 60% at 50% 0%, rgba(201,168,76,0.12) 0%, transparent 70%); pointer-events: none; }
    .hero-badge { display: inline-flex; align-items: center; gap: 8px; padding: 6px 16px; background: rgba(201,168,76,0.08); border: 1px solid var(--border); border-radius: 100px; font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--gold); margin-bottom: 32px; animation: fadeUp 0.6s ease both; }
    .hero-badge::before { content: ''; width: 6px; height: 6px; border-radius: 50%; background: var(--gold); animation: pulse 2s infinite; }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
    .hero h1 { font-family: 'Cormorant Garamond', serif; font-size: clamp(48px, 8vw, 88px); font-weight: 300; line-height: 1.05; letter-spacing: -0.02em; color: var(--text); margin-bottom: 24px; animation: fadeUp 0.6s 0.1s ease both; }
    .hero h1 em { font-style: italic; color: var(--gold); }
    .hero p { font-size: 18px; color: var(--text-muted); max-width: 560px; margin: 0 auto 40px; line-height: 1.7; animation: fadeUp 0.6s 0.2s ease both; }
    .hero-btns { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; animation: fadeUp 0.6s 0.3s ease both; }
    .btn-primary { padding: 16px 32px; background: linear-gradient(135deg, var(--gold), var(--gold-light)); border-radius: 10px; color: var(--dark); font-size: 13px; font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; transition: opacity 0.2s, transform 0.15s; }
    .btn-primary:hover { opacity: 0.9; transform: translateY(-1px); }
    .btn-secondary { padding: 16px 32px; background: transparent; border: 1px solid var(--border); border-radius: 10px; color: var(--text); font-size: 13px; letter-spacing: 0.06em; text-decoration: none; transition: border-color 0.2s; }
    .btn-secondary:hover { border-color: var(--gold); color: var(--gold); }
    .hero-card { margin-top: 64px; display: inline-block; animation: fadeUp 0.6s 0.4s ease both; }
    .card-mockup { background: var(--dark2); border: 1px solid var(--border); border-radius: 16px; padding: 24px 32px; text-align: left; max-width: 320px; box-shadow: 0 40px 80px rgba(0,0,0,0.4); position: relative; }
    .card-mockup::before { content: 'NFC'; position: absolute; top: 16px; right: 16px; font-size: 9px; letter-spacing: 0.2em; color: var(--gold); border: 1px solid var(--border); padding: 3px 8px; border-radius: 100px; }
    .card-avatar { width: 48px; height: 48px; border-radius: 50%; background: linear-gradient(135deg, var(--gold), var(--gold-light)); display: flex; align-items: center; justify-content: center; font-size: 16px; font-weight: 500; color: var(--dark); margin-bottom: 14px; }
    .card-name { font-family: 'Cormorant Garamond', serif; font-size: 20px; font-weight: 300; color: var(--text); }
    .card-title-text { font-size: 11px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); margin-top: 4px; margin-bottom: 16px; }
    .card-stats { display: flex; gap: 20px; padding-top: 16px; border-top: 1px solid var(--border); }
    .card-stat { text-align: center; }
    .card-stat-num { font-family: 'Cormorant Garamond', serif; font-size: 24px; color: var(--gold); }
    .card-stat-label { font-size: 9px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); margin-top: 2px; }

    /* SECTIONS */
    section { padding: 100px 40px; }
    .container { max-width: 1100px; margin: 0 auto; }
    .section-label { font-size: 10px; letter-spacing: 0.3em; text-transform: uppercase; color: var(--gold); margin-bottom: 16px; opacity: 0.8; }
    .section-title { font-family: 'Cormorant Garamond', serif; font-size: clamp(36px, 5vw, 56px); font-weight: 300; line-height: 1.1; color: var(--text); margin-bottom: 20px; }
    .section-title em { font-style: italic; color: var(--gold); }
    .section-desc { font-size: 16px; color: var(--text-muted); line-height: 1.7; max-width: 560px; }

    /* PROBLÈME */
    .problem-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
    .problem-items { display: flex; flex-direction: column; gap: 16px; margin-top: 40px; }
    .problem-item { display: flex; align-items: flex-start; gap: 16px; padding: 20px; background: var(--dark2); border: 1px solid var(--border); border-radius: 12px; }
    .problem-icon { width: 36px; height: 36px; border-radius: 8px; background: rgba(192,57,43,0.1); border: 1px solid rgba(192,57,43,0.2); display: flex; align-items: center; justify-content: center; font-size: 14px; flex-shrink: 0; }
    .problem-text { font-size: 14px; color: var(--text-muted); line-height: 1.6; }
    .problem-text strong { color: var(--text); font-weight: 400; display: block; margin-bottom: 4px; }
    .stat-big { background: var(--dark2); border: 1px solid var(--border); border-radius: 20px; padding: 48px 40px; text-align: center; }
    .stat-big-num { font-family: 'Cormorant Garamond', serif; font-size: 96px; font-weight: 300; color: var(--gold); line-height: 1; }
    .stat-big-text { font-size: 16px; color: var(--text-muted); margin-top: 12px; line-height: 1.5; }
    .stat-big-sub { font-size: 13px; color: var(--text-muted); margin-top: 24px; padding-top: 24px; border-top: 1px solid var(--border); }
    .stat-big-sub strong { color: var(--green); font-weight: 400; }

    /* SOLUTION */
    .solution-bg { background: var(--dark2); }
    .steps-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-top: 60px; }
    .step { position: relative; padding: 28px 24px; background: var(--dark); border: 1px solid var(--border); border-radius: 14px; }
    .step-num { font-family: 'Cormorant Garamond', serif; font-size: 48px; font-weight: 300; color: rgba(201,168,76,0.15); line-height: 1; margin-bottom: 16px; }
    .step-title { font-size: 15px; font-weight: 500; color: var(--gold); margin-bottom: 8px; }
    .step-desc { font-size: 13px; color: var(--text-muted); line-height: 1.6; }

    /* FEATURES */
    .features-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 60px; }
    .feature { padding: 28px; background: var(--dark2); border: 1px solid var(--border); border-radius: 14px; transition: border-color 0.2s; }
    .feature:hover { border-color: rgba(201,168,76,0.4); }
    .feature-icon { font-size: 28px; margin-bottom: 16px; }
    .feature-title { font-size: 15px; font-weight: 500; color: var(--text); margin-bottom: 8px; }
    .feature-desc { font-size: 13px; color: var(--text-muted); line-height: 1.6; }
    .feature-badge { display: inline-block; font-size: 9px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--green); background: rgba(76,175,125,0.1); border: 1px solid rgba(76,175,125,0.2); padding: 2px 8px; border-radius: 100px; margin-top: 10px; }

    /* TARIFS */
    .pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 60px; }
    .pricing-card { padding: 36px 28px; background: var(--dark2); border: 1px solid var(--border); border-radius: 16px; position: relative; }
    .pricing-card.featured { background: var(--dark3); border-color: rgba(201,168,76,0.4); }
    .pricing-badge { position: absolute; top: -12px; left: 50%; transform: translateX(-50%); font-size: 9px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--dark); background: linear-gradient(135deg, var(--gold), var(--gold-light)); padding: 4px 14px; border-radius: 100px; white-space: nowrap; }
    .pricing-name { font-family: 'Cormorant Garamond', serif; font-size: 24px; font-weight: 300; color: var(--text); margin-bottom: 8px; }
    .pricing-desc { font-size: 12px; color: var(--text-muted); margin-bottom: 24px; }
    .pricing-price { display: flex; align-items: flex-end; gap: 4px; margin-bottom: 4px; }
    .pricing-amount { font-family: 'Cormorant Garamond', serif; font-size: 52px; font-weight: 300; color: var(--gold); line-height: 1; }
    .pricing-unit { font-size: 13px; color: var(--text-muted); margin-bottom: 8px; }
    .pricing-sub { font-size: 14px; color: var(--gold-light); font-weight: 400; margin-bottom: 28px; }
    .pricing-features { display: flex; flex-direction: column; gap: 10px; margin-bottom: 28px; }
    .pricing-feature { display: flex; align-items: center; gap: 10px; font-size: 13px; color: var(--text-muted); }
    .pricing-feature::before { content: '✓'; color: var(--green); font-size: 12px; flex-shrink: 0; }
    .pricing-btn { display: block; width: 100%; padding: 14px; text-align: center; border-radius: 10px; font-size: 12px; font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; transition: opacity 0.2s; }
    .pricing-btn-gold { background: linear-gradient(135deg, var(--gold), var(--gold-light)); color: var(--dark); }
    .pricing-btn-outline { background: transparent; border: 1px solid var(--border); color: var(--gold); }
    .pricing-btn:hover { opacity: 0.85; }

    /* CONTACT */
    .contact-bg { background: var(--dark2); }
    .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: start; }
    .contact-info { padding-top: 20px; }
    .contact-detail { display: flex; align-items: center; gap: 14px; margin-top: 24px; }
    .contact-detail-icon { width: 40px; height: 40px; border-radius: 10px; background: rgba(201,168,76,0.08); border: 1px solid var(--border); display: flex; align-items: center; justify-content: center; font-size: 16px; flex-shrink: 0; }
    .contact-detail-text { font-size: 14px; color: var(--text-muted); }
    .contact-detail-text strong { display: block; color: var(--text); font-weight: 400; margin-bottom: 2px; }
    .contact-form { display: flex; flex-direction: column; gap: 14px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
    .form-group { display: flex; flex-direction: column; gap: 6px; }
    .form-label { font-size: 9px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--text-muted); }
    .form-input { padding: 14px 16px; background: var(--dark); border: 1px solid var(--border); border-radius: 10px; color: var(--text); font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 300; outline: none; transition: border-color 0.2s; }
    .form-input::placeholder { color: var(--text-muted); opacity: 0.5; }
    .form-input:focus { border-color: rgba(201,168,76,0.5); }
    .form-textarea { padding: 14px 16px; background: var(--dark); border: 1px solid var(--border); border-radius: 10px; color: var(--text); font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 300; outline: none; resize: vertical; min-height: 120px; transition: border-color 0.2s; }
    .form-textarea:focus { border-color: rgba(201,168,76,0.5); }
    .form-submit { padding: 15px; background: linear-gradient(135deg, var(--gold), var(--gold-light)); border: none; border-radius: 10px; color: var(--dark); font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase; cursor: pointer; transition: opacity 0.2s; }
    .form-submit:hover { opacity: 0.9; }
    .form-success { display: none; padding: 16px; background: rgba(76,175,125,0.1); border: 1px solid rgba(76,175,125,0.2); border-radius: 10px; color: var(--green); font-size: 13px; text-align: center; }
    .form-success.show { display: block; }

    /* FOOTER */
    footer { padding: 48px 40px; border-top: 1px solid var(--border); display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 20px; }
    .footer-logo { font-family: 'Cormorant Garamond', serif; font-size: 20px; font-weight: 400; color: var(--text); }
    .footer-logo span { color: var(--gold); }
    .footer-text { font-size: 12px; color: var(--text-muted); }

    /* ANIMATIONS */
    @keyframes fadeUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      nav { padding: 16px 20px; }
      .nav-links { display: none; }
      section { padding: 70px 20px; }
      .problem-grid, .contact-grid { grid-template-columns: 1fr; gap: 40px; }
      .steps-grid { grid-template-columns: 1fr 1fr; }
      .features-grid { grid-template-columns: 1fr; }
      .pricing-grid { grid-template-columns: 1fr; }
      .form-row { grid-template-columns: 1fr; }
      .hero { padding: 100px 20px 60px; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo"><span>Pro</span>Tap</a>
  <div class="nav-links">
    <a href="#solution">Solution</a>
    <a href="#fonctionnalites">Fonctionnalités</a>
    <a href="#tarifs">Tarifs</a>
    <a href="#contact">Contact</a>
  </div>
  <a href="#contact" class="nav-cta">Demander une démo</a>
</nav>

<!-- HERO -->
<section class="hero">
  <div>
    <div class="hero-badge">Monaco · Côte d'Azur</div>
    <h1>La carte NFC qui transforme<br>chaque contact en <em>prospect qualifié</em></h1>
    <p>Offrez à vos commerciaux un outil de prospection premium — une carte NFC aux couleurs de votre entreprise, connectée à un dashboard de suivi en temps réel.</p>
    <div class="hero-btns">
      <a href="#contact" class="btn-primary">Demander une démo gratuite</a>
      <a href="#solution" class="btn-secondary">Voir comment ça marche</a>
    </div>
    <div class="hero-card">
      <div class="card-mockup">
        <div class="card-avatar">LG</div>
        <div class="card-name">Lilian Giteau</div>
        <div class="card-title-text">Fondateur · Monaco</div>
        <div class="card-stats">
          <div class="card-stat"><div class="card-stat-num">47</div><div class="card-stat-label">Scans</div></div>
          <div class="card-stat"><div class="card-stat-num">18</div><div class="card-stat-label">Prospects</div></div>
          <div class="card-stat"><div class="card-stat-num">38%</div><div class="card-stat-label">Conversion</div></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- PROBLÈME -->
<section id="probleme">
  <div class="container">
    <div class="problem-grid">
      <div>
        <div class="section-label">Le problème</div>
        <h2 class="section-title">La carte papier<br>ne suffit <em>plus</em></h2>
        <p class="section-desc">Chaque rencontre commerciale représente une opportunité. Mais sans outil de suivi, la plupart de ces contacts sont perdus.</p>
        <div class="problem-items">
          <div class="problem-item">
            <div class="problem-icon">✗</div>
            <div class="problem-text"><strong>Aucun suivi possible</strong>Vous ne savez pas si votre carte a été vue, gardée ou jetée.</div>
          </div>
          <div class="problem-item">
            <div class="problem-icon">✗</div>
            <div class="problem-text"><strong>Contacts perdus</strong>Sans système, les coordonnées des prospects s'évanouissent.</div>
          </div>
          <div class="problem-item">
            <div class="problem-icon">✗</div>
            <div class="problem-text"><strong>Manager sans visibilité</strong>Impossible de savoir si vos commerciaux prospectent vraiment.</div>
          </div>
        </div>
      </div>
      <div class="stat-big">
        <div class="stat-big-num">88%</div>
        <div class="stat-big-text">des cartes de visite papier sont jetées en moins d'une semaine</div>
        <div class="stat-big-sub">Avec ProTap, <strong>chaque tap est enregistré</strong> et chaque prospect capturé automatiquement.</div>
      </div>
    </div>
  </div>
</section>

<!-- SOLUTION -->
<section id="solution" class="solution-bg">
  <div class="container">
    <div class="section-label">La solution</div>
    <h2 class="section-title">Un tap suffit pour<br><em>tout déclencher</em></h2>
    <p class="section-desc">ProTap connecte la carte NFC physique à un écosystème digital complet — profil, dashboard, email automatique.</p>
    <div class="steps-grid">
      <div class="step">
        <div class="step-num">01</div>
        <div class="step-title">Tap NFC</div>
        <div class="step-desc">Le prospect approche son téléphone. La page profil s'ouvre instantanément. Aucune app requise.</div>
      </div>
      <div class="step">
        <div class="step-num">02</div>
        <div class="step-title">Profil premium</div>
        <div class="step-desc">Page aux couleurs de votre entreprise avec photo, coordonnées, agenda et catalogue.</div>
      </div>
      <div class="step">
        <div class="step-num">03</div>
        <div class="step-title">Contact capturé</div>
        <div class="step-desc">Le prospect laisse ses informations. Enregistrement automatique dans votre base de données.</div>
      </div>
      <div class="step">
        <div class="step-num">04</div>
        <div class="step-title">Suivi en temps réel</div>
        <div class="step-desc">Dashboard complet avec scans, prospects, taux de conversion et activité mensuelle.</div>
      </div>
    </div>
  </div>
</section>

<!-- FONCTIONNALITÉS -->
<section id="fonctionnalites">
  <div class="container">
    <div class="section-label">Fonctionnalités</div>
    <h2 class="section-title">Tout ce dont vos commerciaux<br><em>ont besoin</em></h2>
    <div class="features-grid">
      <div class="feature">
        <div class="feature-icon">🎨</div>
        <div class="feature-title">Design sur mesure</div>
        <div class="feature-desc">Page profil aux couleurs et au logo de votre entreprise. Chaque commercial a son identité visuelle unique.</div>
        <span class="feature-badge">Exclusif ProTap</span>
      </div>
      <div class="feature">
        <div class="feature-icon">📊</div>
        <div class="feature-title">Dashboard de performance</div>
        <div class="feature-desc">Scans, prospects, taux de conversion, graphique d'activité mensuelle. Tout en temps réel.</div>
      </div>
      <div class="feature">
        <div class="feature-icon">📧</div>
        <div class="feature-title">Email automatique</div>
        <div class="feature-desc">Message personnalisé envoyé au prospect immédiatement après chaque échange.</div>
      </div>
      <div class="feature">
        <div class="feature-icon">👥</div>
        <div class="feature-title">Multi-commercial</div>
        <div class="feature-desc">Chaque commercial a son propre profil, dashboard et base de données entièrement séparés.</div>
      </div>
      <div class="feature">
        <div class="feature-icon">👑</div>
        <div class="feature-title">Dashboard manager</div>
        <div class="feature-desc">Vue globale sur tous vos commerciaux — activité, scans, prospects et statut actif/inactif.</div>
      </div>
      <div class="feature">
        <div class="feature-icon">⚙️</div>
        <div class="feature-title">Back office complet</div>
        <div class="feature-desc">Modification du profil, réseaux sociaux, email automatique et newsletter depuis une interface simple.</div>
      </div>
    </div>
  </div>
</section>

<!-- TARIFS -->
<section id="tarifs" class="solution-bg">
  <div class="container">
    <div class="section-label" style="text-align:center">Tarification</div>
    <h2 class="section-title" style="text-align:center">Simple.<br><em>Transparent.</em></h2>
    <div class="pricing-grid">
      <div class="pricing-card">
        <div class="pricing-name">Solo</div>
        <div class="pricing-desc">Pour un commercial indépendant</div>
        <div class="pricing-price"><span class="pricing-amount">29€</span><span class="pricing-unit">&nbsp;/ carte</span></div>
        <div class="pricing-sub">+ 12€ / mois</div>
        <div class="pricing-features">
          <div class="pricing-feature">1 carte NFC personnalisée</div>
          <div class="pricing-feature">Page profil sur mesure</div>
          <div class="pricing-feature">Dashboard prospects</div>
          <div class="pricing-feature">Email automatique</div>
          <div class="pricing-feature">Back office complet</div>
        </div>
        <a href="#contact" class="pricing-btn pricing-btn-outline">Commencer</a>
      </div>
      <div class="pricing-card featured">
        <div class="pricing-badge">Recommandé</div>
        <div class="pricing-name">Équipe</div>
        <div class="pricing-desc">Pour une équipe de 2 à 5 commerciaux</div>
        <div class="pricing-price"><span class="pricing-amount">22€</span><span class="pricing-unit">&nbsp;/ carte</span></div>
        <div class="pricing-sub">+ 39€ / mois</div>
        <div class="pricing-features">
          <div class="pricing-feature">2 à 5 cartes NFC</div>
          <div class="pricing-feature">Profils individuels</div>
          <div class="pricing-feature">Dashboard manager inclus</div>
          <div class="pricing-feature">Vue d'ensemble équipe</div>
          <div class="pricing-feature">Support prioritaire</div>
        </div>
        <a href="#contact" class="pricing-btn pricing-btn-gold">Commencer</a>
      </div>
      <div class="pricing-card">
        <div class="pricing-name">Entreprise</div>
        <div class="pricing-desc">Pour une équipe de 6 à 20 commerciaux</div>
        <div class="pricing-price"><span class="pricing-amount">18€</span><span class="pricing-unit">&nbsp;/ carte</span></div>
        <div class="pricing-sub">+ 69€ / mois</div>
        <div class="pricing-features">
          <div class="pricing-feature">6 à 20 cartes NFC</div>
          <div class="pricing-feature">Dashboard global</div>
          <div class="pricing-feature">Stats par commercial</div>
          <div class="pricing-feature">Design entreprise unifié</div>
          <div class="pricing-feature">Support dédié</div>
        </div>
        <a href="#contact" class="pricing-btn pricing-btn-outline">Commencer</a>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact" class="contact-bg">
  <div class="container">
    <div class="contact-grid">
      <div class="contact-info">
        <div class="section-label">Contact</div>
        <h2 class="section-title">Demandez votre<br><em>démo gratuite</em></h2>
        <p class="section-desc">Nous vous préparons une carte NFC personnalisée aux couleurs de votre entreprise et une démonstration complète.</p>
        <div class="contact-detail">
          <div class="contact-detail-icon">📧</div>
          <div class="contact-detail-text"><strong>Email</strong>giteaulilian@gmail.com</div>
        </div>
        <div class="contact-detail">
          <div class="contact-detail-icon">📱</div>
          <div class="contact-detail-text"><strong>Téléphone</strong>06 76 25 78 58</div>
        </div>
        <div class="contact-detail">
          <div class="contact-detail-icon">📍</div>
          <div class="contact-detail-text"><strong>Zone</strong>Monaco · Côte d'Azur</div>
        </div>
      </div>
      <div>
        <form class="contact-form" onsubmit="submitContact(event)">
          <div class="form-row">
            <div class="form-group">
              <label class="form-label">Prénom et nom *</label>
              <input type="text" class="form-input" id="contactNom" placeholder="Jean Dupont" required>
            </div>
            <div class="form-group">
              <label class="form-label">Entreprise</label>
              <input type="text" class="form-input" id="contactEntreprise" placeholder="AFIM France">
            </div>
          </div>
          <div class="form-group">
            <label class="form-label">Email *</label>
            <input type="email" class="form-input" id="contactEmail" placeholder="jean@entreprise.com" required>
          </div>
          <div class="form-group">
            <label class="form-label">Téléphone</label>
            <input type="tel" class="form-input" id="contactTel" placeholder="06 XX XX XX XX">
          </div>
          <div class="form-group">
            <label class="form-label">Message</label>
            <textarea class="form-textarea" id="contactMessage" placeholder="Parlez-nous de votre équipe commerciale..."></textarea>
          </div>
          <button type="submit" class="form-submit">Envoyer ma demande</button>
          <div class="form-success" id="contactSuccess">✓ Votre demande a été envoyée ! Nous vous recontactons sous 24h.</div>
        </form>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo"><span>Pro</span>Tap</div>
  <div class="footer-text">© 2025 ProTap · Monaco · Tous droits réservés</div>
  <div class="footer-text">Développé avec ❤️ à Monaco</div>
</footer>

<script>
function submitContact(e) {
  e.preventDefault();
  var nom = document.getElementById('contactNom').value;
  var entreprise = document.getElementById('contactEntreprise').value;
  var email = document.getElementById('contactEmail').value;
  var tel = document.getElementById('contactTel').value;
  var message = document.getElementById('contactMessage').value;

  var SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbwTeme25bmbkxTz9mBrd0Y7UTwvENrSD-UbHgVPjABAVQ-ehT4u4IqoQo2djlNm63HT4A/exec';
  var url = SCRIPT_URL
    + '?nom=' + encodeURIComponent(nom + (entreprise ? ' (' + entreprise + ')' : ''))
    + '&telephone=' + encodeURIComponent(tel)
    + '&email=' + encodeURIComponent(email)
    + '&commercial=Landing+ProTap'
    + '&id=lilian';

  var img = new Image();
  img.src = url;

  document.getElementById('contactSuccess').classList.add('show');
  e.target.reset();
}
</script>

</body>
</html>
